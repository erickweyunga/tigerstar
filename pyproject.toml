[project]
name = "tigerstar"
version = "0.1.0"
description = "Double-entry ledger engine backed by Firestore"
requires-python = ">=3.13"
dependencies = [
    "firebase-admin>=6.0",
]

[tool.pyright]
include = ["src"]
